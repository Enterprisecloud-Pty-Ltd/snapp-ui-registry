{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "snapp-search-utils",
  "title": "Snapp Search Utilities",
  "description": "Shared normalization and multi-word matching helpers for feature-owned search fields.",
  "files": [
    {
      "path": "registry/radix-nova/lib/search.ts",
      "content": "function normalizeSearch(value: string) {\n  return value.trim().toLocaleLowerCase()\n}\n\nfunction matchesSearch(query: string, values: string[]) {\n  const normalizedQuery = normalizeSearch(query)\n  if (!normalizedQuery) return true\n\n  const searchableValue = normalizeSearch(values.join(\" \"))\n  return normalizedQuery\n    .split(/\\s+/)\n    .every((word) => searchableValue.includes(word))\n}\n\nexport { matchesSearch, normalizeSearch }\n",
      "type": "registry:lib",
      "target": "@lib/search.ts"
    }
  ],
  "type": "registry:lib"
}