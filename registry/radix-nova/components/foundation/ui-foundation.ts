/**
 * Marker installed by `snapp-ui-foundation`.
 *
 * The registry item carries every canonical Shadcn UI primitive as registry
 * dependencies; feature code should continue importing the individual
 * primitives it uses.
 */
export const snappUiFoundation = true as const
